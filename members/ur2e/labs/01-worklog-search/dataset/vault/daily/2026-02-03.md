# 2026-02-03

- `cp-03` API Server가 안 뜬다는 제보. 로그에 `x509: certificate has expired` 반복. 결론은 인증서 갱신 때 이 노드만 빠진 거였다. → [[INC-003-apiserver-etcd-tls-expiry]]
- 처음에 노드 시간 동기화 문제인가 싶어서 시간부터 봤는데 정상이었다. x509 만료 뜨면 시계보다 인증서 유효기간을 먼저 보는 게 맞다.
- 갱신 명령을 매번 검색해서 찾는 게 짜증나서 절차를 메모로 박아놨다 → [[kubeadm-인증서-갱신-절차]]
- 나머지 control-plane 노드 인증서 만료일도 한 번에 뽑아봤다. `cp-01` 은 4월, `cp-02` 는 6월. 만료 60일/30일 전 알람 걸어달라고 요청해둠. #todo
- 잡일: 사내 위키에 있던 오래된 런북 3개 정리해서 링크만 남김.

#인증서 #apiserver #운영
