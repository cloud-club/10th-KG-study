---
title: 로컬 인프라 (docker compose)
type: entity
tags: [runbook, tooling]
status: maintained
created: 2026-09-16
updated: 2026-09-16
members: [sese2204, lys0611, dldusgh318, e0ng, yujeong430, do-dop]
---

> 실습용 DB 세 개(PostgreSQL 17 + pgvector, Elasticsearch 9 + nori, Neo4j 5.26 + APOC)를 `docker compose up -d` 한 번에 띄우는 공용 환경. 사용법의 원본은 `infra/README.md`(PR #6, 미머지)이고, 이 페이지는 거기서 겪은 함정과 판단, 그리고 공용을 안 쓴 멤버들의 구성을 모은다.

## 기준 · 루틴

| 서비스 | 이미지 | 접속 | 용도 |
|---|---|---|---|
| PostgreSQL 17 + pgvector | `pgvector/pgvector:pg17` | `postgresql://kg:kg@localhost:5432/kg` | 청크·메타데이터·임베딩 저장, 벡터 검색, pg_trgm |
| Elasticsearch 9 + nori | `infra/elasticsearch/Dockerfile` | `http://localhost:9200` (인증 없음) | BM25 / 하이브리드, 한국어 형태소 |
| Neo4j 5.26 LTS + APOC | `neo4j:5.26-community` | `bolt://localhost:7687`, Browser 7474 (neo4j / kgstudy2026) | 지식그래프 |
| Kibana (선택) | `kibana:9.1.0`, `--profile ui` | `http://localhost:5601` | ES 인덱스 탐색 |

- `cp .env.example .env` → `docker compose up -d` → `bash infra/check.sh`("모두 정상입니다"). 최초 실행은 이미지 다운로드 + ES 플러그인 빌드로 몇 분. 메모리 4GB 이상 권장(ES 1GB + Neo4j 1.5GB + PG), 8GB 노트북이면 `ES_JAVA_OPTS=-Xms512m -Xmx512m`.
- 데이터는 레포 루트 `data/<id>/`에 두면 컨테이너에서 바로 읽힌다: PG `/data/<id>/x.csv`(`COPY`), Neo4j `file:///<id>/x.csv`(`LOAD CSV`). ES는 마운트 없이 `_bulk`. `data/`는 README 빼고 git이 무시한다.
- `docker compose down`은 데이터 유지, `down -v`는 초기화(되돌릴 수 없음). 여러 실습이 한 인스턴스를 쓰므로 테이블·인덱스에 멤버 접두사를 붙인다(`do_dop_kakao_chunks`).
- `.env`의 `OPENAI_API_KEY`는 현황판 로컬 빌드와 실습에서 선택적으로 쓴다 → [[현황판]].

## 겪은 문제 · 배운 것

- **pg_trgm이 한글에 안 먹힘** (`show_trgm('한글')` = `{}`): DB가 순수 `C` 로케일로 생성된 것. `datctype`이 `C.UTF-8`이어야 한다. sese2204가 카카오톡 실습에서 겪고 compose를 `C.UTF-8`로 고쳤다 (2026-09-09) → [[PostgreSQL과-pgvector-함정]].
- 이미 만들어진 볼륨에는 `infra/postgres/init/01-extensions.sql`이 자동 적용되지 않는다. Neo4j 비밀번호 오류는 볼륨에 이전 비밀번호가 남은 것.
- ES가 계속 재시작 → 메모리 부족(`OutOfMemory`). Linux는 `vm.max_map_count=262144`. Colima는 기본 2GiB라 `--memory 6` 이상 (lys0611) → [[Elasticsearch-운영-함정]].
- `port is already allocated` → 로컬에 이미 PG/ES/Neo4j가 돌고 있다. M1/M2 Mac은 세 이미지 모두 arm64 지원.

## 공용을 안 쓴 멤버들

| 멤버 | 구성 | 공용과의 차이 |
|---|---|---|
| dldusgh318 | 자체 compose: ES 8.15.1 + nori, `pgvector/pgvector:pg16`, 호스트 5433 | 로컬 Postgres가 5432를 써서 비킴 |
| e0ng | 자체 compose: ES 8.15.0 + nori, PG16 + pgvector | 버전 불일치 |
| yujeong430 | 자체 compose: ES 9201, PG 5433 | 포트 불일치 |
| lys0611 | 자체 compose: `pgvector/pgvector:pg18`(18.6, pgvector 0.8.6), ES 9.5.3 + nori, Colima 4CPU/6GiB | pg18은 볼륨을 `/var/lib/postgresql`에 마운트해야 함. 공용(pg17, ES 9.1)으로 옮기려면 `pg_dump`나 재적재 필요 |

- heebindev는 PR #12에서 자체 compose를 버리고 공용으로 옮겼다. 공용을 쓰는 멤버: sese2204, do-dop, kdyann, heebindev, ur2e.
- 버전이 다르면 클라이언트 라이브러리 메이저도 맞춰야 한다(`elasticsearch>=9,<10`) (lys0611).

## 관련

- [[PostgreSQL과-pgvector-함정]] · [[Elasticsearch-운영-함정]] · [[HNSW와-pgvector-인덱스]] · [[지식그래프와-온톨로지]] (Neo4j) · [[현황판]] · [[실습-비교표]]

## 출처

- sese2204 · infra/README.md, docker-compose.yml — [infra/README.md](../../infra/README.md), [docker-compose.yml](../../docker-compose.yml) (PR #6 미머지); 레포 `README.md` "로컬 인프라" 절, `data/README.md`
- lys0611 · GUIDE §2, RUNBOOK §1·§5, 실습 02 (§배운 점 이관 함정) — [members/lys0611/labs/01-ingest/GUIDE.md](../../members/lys0611/labs/01-ingest/GUIDE.md), [RUNBOOK_mac_colima.md](../../members/lys0611/labs/01-ingest/RUNBOOK_mac_colima.md), [labs/02-hybrid-rag-agent/README.md](../../members/lys0611/labs/02-hybrid-rag-agent/README.md) (PR #15 미머지)
- dldusgh318 · WEEK2 (compose) — [members/dldusgh318/labs/01-three-generations/WEEK2.md](../../members/dldusgh318/labs/01-three-generations/WEEK2.md); e0ng · 실습 (§환경) — [members/e0ng/labs/02-search-generations/README.md](../../members/e0ng/labs/02-search-generations/README.md); yujeong430 · 실습 — [members/yujeong430/labs/01-kakaotalk-search/README.md](../../members/yujeong430/labs/01-kakaotalk-search/README.md); do-dop · 실습 (§환경) — [members/do-dop/labs/02-kakaotalk-search/README.md](../../members/do-dop/labs/02-kakaotalk-search/README.md); heebindev · PR #12 — [members/heebindev/labs/01-notion-search/README.md](../../members/heebindev/labs/01-notion-search/README.md)
