# YYYY-MM-DD

프론트매터 없이 쓴다. 날짜는 파일명에서 나온다.

- 시간 + 무슨 일 + 어떻게 됐는지. 한 줄이어도 된다.
- 별도 문서로 정리한 건 `[[문서명]]` 으로 링크만 걸고 내용은 중복하지 않는다.
- 나중에 할 일은 `#todo` 를 붙인다. 흩어져 있어도 태그로 모을 수 있다.
- 아무 일 없던 날도 한 줄 남긴다. 비어 있는 날짜와 조용했던 날짜는 다르다.

#태그 #태그
