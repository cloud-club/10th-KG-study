# 10th-KG-study — 스터디 LLM 위키 스키마·운영 지침

이 저장소는 클라우드클럽 10기 KG(Knowledge Graph) 스터디의 작업 공간이자, **LLM이 유지·관리하는 스터디
지식베이스**다. 멤버는 `members/<github-id>/`에 노트와 실습을 쌓고, 에이전트는 그것을 읽어 `wiki/`에
증류한다. `wiki/`는 옵시디언 볼트이며 PARA(실행 가능성 기준)로 나눈다. 사람은 소재를 던지고 질문하고
검토한다. **`wiki/`의 모든 페이지는 에이전트가 쓰고 고치고 연결한다.** 이 파일이 스키마다. 관례가
바뀌면 대화가 아니라 이 파일을 고친다.

패턴의 원문(철학)은 [[LLM-위키-패턴]]에 있다. 한 번 읽고, 규칙은 이 파일을 따른다.

## 목적

멤버 12명이 같은 주제(검색의 세 세대, 하이브리드 검색, RAG, 지식그래프)를 각자 폴더에 따로 정리한다.
위키는 그 사일로를 **주제별로 합성**한다. 누가 무엇을 어떤 조건에서 확인했는지, 어디서 서로 다른 결론이
났는지, 어떤 도구에 어떤 함정이 있는지. 멤버의 노트는 *소재*고, 거기서 증류한 페이지가 *위키*다.

## 세 층

1. **소재** — `members/<id>/`의 노트(`notes/*.md`)·실습 README(`labs/*/README.md` 등 실습 문서)·
   `readings.md`, 머지 대기 PR, `wiki/0-pending/`(스터디장이 막 쓰는 메모). **`members/`는 불변이다.**
   에이전트는 읽기만 하고 절대 고치지 않는다(자기 노트는 각자 스터디 작업으로 쓴다). `protect-raw.py`
   훅이 다른 멤버 폴더 편집을 막는다.
2. **위키** — `wiki/1-projects/`, `wiki/2-areas/`, `wiki/3-resources/`, `wiki/4-archives/`의 페이지.
   에이전트가 소유한다. 사람은 읽고, 고칠 게 있으면 자기 노트를 고친 뒤 재-ingest를 요청한다.
3. **스키마** — 이 `CLAUDE.md`. 사람과 함께 진화한다.

## 디렉터리 구조

```
CLAUDE.md                  # 스키마 (이 파일)
members/<github-id>/       # 소재 — 불변. README(소개) · readings.md · notes/ · labs/
members/names.json         # id → 이름, members/cohorts.json — 반 명단 (현황판·위키가 같이 쓴다)
wiki/                      # 옵시디언 볼트 = 이 폴더
  index.md                 # 전체 카탈로그 — 질문에 답할 때 가장 먼저 읽는다
  log.md                   # append-only 활동 로그
  _templates/              # 새 페이지의 출발점 (project · area · resource · note)
  0-pending/               # 분류 전 메모. 스터디장이 자유롭게 쓴다. 정리 요청 시 아래로 분류
  1-projects/              # 끝이 있는 일. 기수 운영, 공용 골든셋 같은 것. <슬러그>/<슬러그>.md(허브) + 하위 페이지
  2-areas/                 # 끝이 없는 책임. 로컬 인프라, 현황판, 위키 운영
  3-resources/             # 개념·비교·함정 페이지. 위키의 본체. 스터디 노트 지도, 패턴 원문도 여기
  4-archives/              # 비활성. 끝난 기수(완료일 접두), 낡은 페이지
  .obsidian/               # 볼트 설정 (workspace 등 개인 상태는 git 무시)
dashboard/ scripts/ infra/ # 현황판·인프라 코드. 위키 아님. 규칙은 README.md · CONTRIBUTING.md
```

**PARA 판별**: 완료 시점이 있으면 project(기수, 골든셋 구축), 매일·매주 반복되면 area(인프라, 현황판),
나중에 찾아보려 두면 resource(개념, 비교, 함정), 한 달 안 열지 않았으면 archive. 스터디 지식은 대부분
resource다. 헷갈리면 "이 페이지를 언제 다시 꺼내 보는가"로 정한다.

**소재 범위**: `members/<id>/notes/**/*.md`, `members/<id>/labs/**/*.md`(README·WEEK·RESULT 등 실습 문서),
`members/<id>/readings.md`, `members/<id>/README.md`(소개·목표만). 실습 데이터(txt·jsonl·csv), 발표용
html, 코드는 소재가 아니다 — 실습 문서가 코드를 설명한 만큼만 위키에 들어간다. 폴더명이 규칙과 달라도
(`note/`, `week2/` 하위 폴더) 소재다.

**머지 대기 PR**: 아직 `main`에 없는 노트도 소재로 쓸 수 있다. 그때는 `## 출처`에 `PR #번호 (미머지)`를
붙이고, 머지되면 표시를 지운다. 머지 여부는 `gh pr list`로 확인한다.

## 페이지 관례

**파일명**: 한국어 자연어 이름, 띄어쓰기는 `-`. 약어·고유명사(BM25·RRF·HNSW·pgvector·nori)는 그대로.
안정적으로, 이름 변경은 링크를 깨뜨린다. 볼트 안에서 basename이 유일해야 한다(위키링크가 basename만 쓴다).

**위키링크**: 옵시디언 `[[basename]]`(경로·확장자 없음). 페이지에서 이름이 나오는 개념·도구·실습·규칙은
전부 링크한다. 아직 없는 페이지로의 링크는 "만들 가치가 있는 페이지"의 표시로 허용한다. `members/`의
소재는 볼트 밖이라 위키링크가 아니라 `## 출처`의 저장소 상대경로 링크로 가리킨다.

**Frontmatter(YAML)** — 모든 페이지 필수. 옵시디언 속성·검색·Bases의 기반이다.

```yaml
---
title: 사람이 읽는 제목
type: project | area | resource | note
tags: [concept]                  # 하위 구분. concept | comparison | pitfall | runbook | rule | methodology | glossary | map …
status: stub | draft | maintained | done | archived
created: 2026-09-16
updated: 2026-09-16
# 선택 키
members: [sese2204, kdyann]      # 이 페이지에 소재를 제공한 멤버 github id
weeks: [2, 3]                    # 관련 스터디 주차
source: https://…                # 외부 원문이 있으면 (패턴 원문 등)
---
```

키 안전: `title:`은 페이지당 1개. `tags`·`aliases`·`cssclasses`는 옵시디언 용도로만. 일반 단어 키는
피하고 명시적 키를 쓴다. 같은 키를 두 번 쓰지 않는다. `members`의 id는 `members/` 폴더명과 같게 쓴다.

**본문 형태**:
- 맨 위 한 줄 정의(`>` 인용).
- 본문. 개념 페이지: 정의 → 동작 원리 → 강점·약점 → 멤버들이 확인한 것(실험 수치는 조건과 함께,
  누가 어떤 데이터로) → 함정 → 열린 질문. 프로젝트 페이지: 배경·목표 → 타임라인(`날짜·구분·이벤트/
  목표·목표일·상태` 표) → 스코프 → 의사결정·이슈 → 남은 일.
- `## 관련` — 이웃 페이지 위키링크.
- `## 출처` — 소재 목록. 한 줄에 `멤버 · 문서 제목 — [저장소 경로](../../members/…)` 형식. 미머지 PR이면
  `(PR #n 미머지)`. 외부 논문·문서는 제목과 URL. **있는 것만**, 불확실한 링크는 적지 않는다.
- 첨부 파일은 페이지 옆 `_attachments/`에 둔다. `raw`·`sources` 이름은 쓰지 않는다.

**멤버 표기**: github id로 적는다(`kdyann`). 이름은 `members/names.json`에 있으니 필요하면 병기한다.
누가 무엇을 확인했는지가 위키의 핵심 정보이므로 수치·주장에는 멤버를 붙인다.

**인용과 정직**: 자명하지 않은 사실은 출처가 있어야 한다. 없으면 `> TODO: unverified`로 표시하고
지어내지 않는다. 멤버 노트끼리 다르게 말하면 `> ⚠️ Contradiction:`으로 양쪽을 출처와 함께 드러낸다.
노트의 주장을 위키가 검증한 것처럼 쓰지 않는다 — "X가 Y라고 정리했다"와 "X가 Y를 측정했다"를 구분한다.

**날짜**: 상대 표현("이번 주", "다음 주차")은 소재의 `date`나 커밋 날짜 기준으로 푼다. 확정되지 않은
날짜는 `경`·`~`·`잠정` 마커와 근거를 함께 적는다.

**민감 정보**: 토큰·비밀값·개인 대화 원문(카카오톡·노션·인스타그램 내용)은 적지 않는다. 실습 데이터의
통계(건수·기간)는 되지만 대화 내용·이름·장소는 옮기지 않는다. 멤버는 github id로만 부른다.

## 운영

### Ingest (소재를 위키로)

PR이 머지되었거나, 스터디장이 `wiki/0-pending/`에 메모를 두거나, 특정 노트·PR을 가리키며 정리를 요청하면:

1. **읽는다.** 소재 전체를 읽는다. 크면 읽기 전용 서브에이전트로 나눠 읽고 쓰기는 직접 한다.
2. **분류한다.** project · area · resource 중 어디인지 정한다. 해당 템플릿에서 페이지를 만들거나
   기존 페이지를 갱신한다. **비슷한 페이지가 있으면 새로 만들지 말고 갱신한다** — 먼저 `index.md`와
   [[스터디-노트-지도]]를 검색한다.
3. **통합한다.** 영향받는 모든 페이지를 갱신하고 **양방향** 위키링크를 건다. 개념 페이지의 "멤버들이
   확인한 것"에 새 멤버의 결과를 붙이고, [[스터디-노트-지도]]에 소재 → 페이지 대응을 적는다.
4. **모순을 표시한다.** 새 노트가 기존 페이지와 다르게 말하면 어디서 그런지 적는다.
5. **`index.md`를 갱신한다.** 새 페이지는 카탈로그에 한 줄.
6. **`log.md`에 append한다.**
7. **요약한다.** 무엇을 만들고 바꿨는지, 열린 질문, 아직 증류 안 된 소재.

`wiki/0-pending/`의 메모는 위키에 반영한 뒤 삭제한다. `members/`의 소재는 절대 삭제·수정하지 않는다.

### Query (질문에 답하기)

1. `wiki/index.md`를 먼저 읽어 관련 페이지를 찾고 들어간다. 위키에 없으면 [[스터디-노트-지도]]로
   소재를 찾아 읽는다.
2. 사용한 페이지를 위키링크로, 소재는 경로로 인용하며 답한다.
3. **좋은 답은 위키에 되돌려 넣는다.** 비교·분석·새로 발견한 연결은 내구성 있는 지식이다. 채팅에서
   죽게 두지 말고 페이지로 만들거나 갱신하고 로그에 남긴다.

### Lint (건강 점검)

"위키 린트" 요청 시 점검하고 보고한다: 페이지 간 모순, 새 소재가 대체한 낡은 주장, 고아 페이지(들어오는
링크 없음)·막다른 페이지(나가는 링크 없음), 아직 없는 페이지로의 링크, 여러 페이지에서 언급되지만 자기
페이지가 없는 개념, `status: stub`·`TODO` 방치, 증류되지 않은 소재([[스터디-노트-지도]]에 없는
`members/` 문서), 머지되어 `(PR #n 미머지)` 표시를 지워야 할 출처, 소재 태그의 표기 불일치(예:
`vector-search`/`embedding`/`hnsw`가 같은 뜻으로 쓰임). 수정안을 제시하고 사용자가 동의하면 적용한다.
린트도 로그에 남긴다.

### Archive (기수 닫기)

기수가 끝나면: 다시 쓸 지식은 `3-resources/`에 남기고, 기수 운영 프로젝트 폴더를 통째로
`4-archives/<완료일>-<슬러그>/`로 옮긴다. frontmatter `status: archived`. `index.md`의 항목을 보관 절로
옮기고 로그에 남긴다.

## index.md 형식

폴더(Pending · Projects · Areas · Resources · Archives)별 카탈로그. 한 줄에 `- [[basename]] — 한 줄 요약`.
질문에 답할 때 파일 검색보다 먼저 읽는 탐색 표면이므로 모든 ingest에서 갱신한다. `members/`의 소재는
개별로 싣지 않고 [[스터디-노트-지도]] 한 줄만 둔다. 아직 안 쓴 페이지는 링크 없이 평문으로 적는다.

## log.md 형식

Append-only, 최신이 아래. `grep "^## \[" wiki/log.md | tail -5`로 최근 활동이 보이게 접두를 맞춘다.

```
## [2026-09-16] ingest | <소재 제목 또는 PR #n>
- created: 3-resources/<페이지>
- updated: 3-resources/<페이지> (무엇을 추가), index
- flagged: A와 B의 모순
```

동사: `setup` · `ingest` · `query` · `lint` · `refactor` · `archive`. 날짜는 `[YYYY-MM-DD]`.

## 운영 규칙

- `wiki/` 페이지는 에이전트가 소유한다. `members/`는 읽기만 한다 — 다른 멤버 폴더는 훅이 막고, 자기
  폴더도 위키 작업 중에는 건드리지 않는다.
- 새로 만들기보다 **기존 페이지 갱신**을 우선한다. 먼저 검색한다.
- `wiki/index.md`·`wiki/log.md`를 항상 최신으로 — 다음 세션과 사람이 그걸로 길을 찾는다.
- 명확하고 평이하게, 기존 페이지의 밀도와 어조에 맞춰 쓴다. 노트의 문장을 그대로 복사하지 말고 합성한다.
- ingest는 일괄 지시가 없으면 사용자와 가까이 붙어 진행하고, 결정은 묻어 두지 않고 드러낸다.
- 관례가 바뀌면 이 파일을 고친다. 새 페이지는 `wiki/_templates/`에서 시작한다.
- 커밋·푸시는 요청이 있을 때만 한다. 위키 변경은 `main` 직접 푸시가 아니라 PR로 올린다(CONTRIBUTING).

## 강제 장치 (훅)

`.claude/settings.json`의 훅 세 개가 하네스 수준에서 규칙을 지킨다(`.claude/hooks/README.md`).

- **`session-rules.sh`** (`SessionStart`) — 매 세션 핵심 규칙을 컨텍스트에 주입한다.
- **`protect-raw.py`** (`PreToolUse: Edit|Write|MultiEdit|NotebookEdit`) — `members/<다른 멤버>/` 아래
  파일 편집을 막는다. 본인 폴더(`gh api user`의 로그인, `.cache/gh-login`에 캐시)는 허용한다.
- **`check-bookkeeping.py`** (`Stop`) — 위키 페이지를 바꾸고 `wiki/log.md`를 안 건드렸거나, 새 페이지를
  만들고 `wiki/index.md`를 안 건드렸으면 한 번 멈춰 세운다. `0-pending/`·템플릿은 면제.

훅을 고친 뒤에는 `/hooks`를 한 번 열거나 재시작해야 반영된다.

## 현황판·인프라와의 관계

- **현황판**(`dashboard/`, `scripts/build_dashboard.py`)은 사람·활동 중심이다(누가 얼마나). 위키는 지식
  중심이다(무엇을). 둘 다 `members/`를 읽는다. 현황판 지식그래프의 주제 노드는 노트 frontmatter `tags`에서
  나오므로, 위키 개념 페이지의 파일명·`tags`를 그 태그와 맞추면 서로 잇기 쉽다.
- 현황판 스캐너는 `notes/*.md`·`labs/*/README.md`만 읽는다. 위키 ingest는 그보다 넓게(`notes/**`,
  `labs/**/*.md`, `note/`) 읽으므로, 스캐너가 놓치는 소재를 발견하면 린트 보고에 적는다.
- **로컬 인프라**(`docker-compose.yml`, `infra/`) 사용법은 `infra/README.md`가 원본이고, 위키의
  [[로컬-인프라]]는 거기서 겪은 함정과 판단을 모은다.

## 도구 메모

- **옵시디언**이 사람의 리더다(볼트 = `wiki/`). 위키링크·그래프·속성이 살아 있으니 그에 맞춰 쓴다.
  GitHub에서도 읽히므로 위키링크 외의 링크는 상대경로 마크다운 링크로 쓴다.
- 이 폴더는 **git** 저장소다. `.env`(OPENAI_API_KEY 등)는 무시 목록에 있다. 위키는 LLM 에이전트
  세션(Claude Code 등)이 직접 쓴다 — 별도 빌드 스크립트는 없다.
